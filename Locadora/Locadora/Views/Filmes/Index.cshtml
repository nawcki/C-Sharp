@model IEnumerable<Locadora.Models.Filmes>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "AdicionaUsuario")
</p>
<table class="table">
    <tr>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>@Html.DisplayFor(modelItem => item.Name)</td>
    <td>
        
        @Html.ActionLink("Edit", "Atualiza", new {  /*id=item*/  }) |
        @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
        @Html.ActionLink("Delete", "Deleta", new { id = item.Name })
    </td>
</tr>
}

</table>
